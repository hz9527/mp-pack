const resolveNpm = require('./lib/resolveNpm');
const projectReplace = require('./lib/projectReplace');
const multiple = require('./lib/multiple');

module.exports = {
  resolveNpm,
  projectReplace,
  multiple
};
